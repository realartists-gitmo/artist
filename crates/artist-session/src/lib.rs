//! WIPED STUB — artist-session.
//!
//! WIPE FLAG: this crate was flagged as a wipe candidate when the kernel and
//! component crates were wiped on 2026-08-17, and has now been wiped to an
//! empty skeleton. The previous refactor ignored substantial prior art; rebuild
//! this against prior art before writing code.
//!
//! Nothing in `src/` survives; this crate is an empty skeleton. `Cargo.toml`
//! is intentionally left intact (deps + workspace wiring) so the rebuild has
//! its original dependency picture available.
//!
//! TODO(rebuild): design the new session store against prior art before writing code.