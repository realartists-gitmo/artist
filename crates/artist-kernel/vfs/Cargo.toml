[package]
name = "artist-vfs"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
artist-kernel = { path = ".." }
async-trait = "0.1"
fuser = { version = "0.18", features = ["experimental"] }

[dev-dependencies]
tempfile = "3"