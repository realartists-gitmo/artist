[package]
name = "artist-vfs-windows"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
artist-kernel = { path = "../.." }
async-trait = "0.1"

[target.'cfg(windows)'.dependencies]
winfsp = "0.13"
winfsp-sys = "0.12"
futures = "0.3"

[target.'cfg(windows)'.build-dependencies]
winfsp = "0.13"