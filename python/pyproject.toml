[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "artist-repl"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fff-search>=0.10.5",
]

[tool.maturin]
manifest-path = "../crates/artist-python/Cargo.toml"
module-name = "_artist_bridge"
