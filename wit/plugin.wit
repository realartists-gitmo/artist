package artist:harness@0.1.0;

world artist-plugin {
    /// One stable JSON boundary keeps plugin-specific functions out of model tool schemas.
    export call: func(operation: string, payload-json: string) -> result<string, string>;
}
