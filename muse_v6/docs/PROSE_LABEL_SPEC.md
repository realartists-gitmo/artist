# Superseded prose-v4 specification

`muse-prose-label-4.1` is superseded by `muse-semantic-label-6`.

The old contract incorrectly excluded structured tool calls/results from the learned target. Do not use it for new labeling. The active normative specification is `docs/SEMANTIC_LABEL_SPEC.md`.

The historical 36-window prose fixture remains a regression subset under `audit/prose-v4/`; passing that subset alone is not pre-label readiness.
